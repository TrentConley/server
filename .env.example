# --- OIDC Server Configuration ---
# This file is an example. Copy it to .env and fill in your actual values.
# Do NOT commit the .env file to version control.

# The base URL of the OIDC provider (e.g., Google, Okta, Auth0)
OIDC_PROVIDER_URL="https://accounts.google.com"

# Your application's Client ID from the OIDC provider
CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"

# Your application's Client Secret from the OIDC provider
CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# The public base URL of this server. For local dev, it's http://localhost:8000.
# For Railway, it will be your public app URL (e.g., https://my-app.up.railway.app)
APP_BASE_URL="http://localhost:8000"

# The unique ID of your VSCode extension that will receive the callback.
VSCODE_EXTENSION_ID="YOUR_VSCODE_EXTENSION_ID"

# --- Optional SSL Configuration (for standalone deployments) ---
# If you provide a key and cert file, the production server will run in HTTPS mode.
# Leave these blank for environments like Railway that handle SSL for you.
SSL_KEYFILE=""
SSL_CERTFILE=""
